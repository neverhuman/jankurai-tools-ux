#!/usr/bin/env bash
# Mandatory pre-push gate. Runs the same quality gates CI runs so broken code
# cannot be pushed. Wire it once with:
#   git config core.hooksPath ops/git-hooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

bash ops/ci/quality-gates.sh
